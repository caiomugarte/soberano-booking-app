# Quick Task 001 Summary

- Moved the "hide past absences" rule into `GET /admin/schedule/absences` so the API is the source of truth.
- Simplified the admin schedule page to render the returned absence list directly.
- Verified the change with successful API and web production builds.
