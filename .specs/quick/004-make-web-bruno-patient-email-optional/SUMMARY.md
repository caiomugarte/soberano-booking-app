# Quick Task 004 Summary

- Normalized the patient form email before submit.
- Create mode now omits blank email values instead of sending an empty string.
- Edit mode now submits `null` for a blank email so an existing email can be cleared.
